{
  "name": "venforce-frontend-react",
  "version": "1.0.0",
  "private": true,
  "description": "Primeira migração gradual do Portal VenForce para React + Vite. Escopo: apenas a Cliente 360.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/clean-assets.mjs && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "dev:full": "vite --config vite.full.config.js",
    "build:full": "node scripts/clean-assets-full.mjs && vite build --config vite.full.config.js"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.6.3",
    "@testing-library/react": "^16.1.0",
    "@testing-library/user-event": "^14.5.2",
    "@vitejs/plugin-react": "^4.3.4",
    "jsdom": "^25.0.1",
    "vite": "^6.0.7",
    "vitest": "^2.1.8"
  }
}
